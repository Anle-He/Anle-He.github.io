# Contributing

Before submitting a change, run:

```bash
npm install
npm run validate
npm run lint
npm run audit
npm run build
```

Do not include secrets, private personal data, or third-party media without permission.
Contributions are accepted under `GPL-3.0-only`.
